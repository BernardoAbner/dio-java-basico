package dio.bernardo.palavras;

public class palavrasReservadas {

    //modificadores de acesso:
        //public: acesso de qualquer classe
        //private: acesso apenas dentro da classe
        //protected: acesso por classes no mesmo pacote e subclasses. uso de herança
    
    //modificadores de classes:
        //abstract: classe que não pode ser instanciada ou método que precisa ser implemetado por uma subclasse não abstrata
        //class: especifica uma classe 
        // extends: indica a superclasse que a subclasse está estendendo
        // final:  impossibilita que uma subclasse seja estendida, que um método seja sobrescrito ou que uma váriavel seja reinicializada
        //implements: indica as interfaces que uma subclasse irá implementar 
        //interface: especifica uma interface
        //native: indica que um método está escrito em uma linguagem dependendo da plataforma, como o C
        //new: cria um novo objeto, chamando seu construtor
        //static: faz um método ou variável pertencer a classe so invés de às instancias 
        //synchronized: indica que um método só pode ser acessado por uma thread de cada vez
        //transient: impede a serialização  de campos
    
}
